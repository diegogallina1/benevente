[pytest]
# The default base temporary directory under %TEMP% is not writable in this
# environment, which made 29 tests error during collection for a reason that
# has nothing to do with the code under test. Keeping the base inside the
# repository's scratch area makes the suite runnable and self-contained.
addopts = --basetemp=work/pytest_tmp
testpaths = tests
